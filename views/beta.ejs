<!DOCTYPE html>
<html>
  <head>
    <% include head.common.ejs %> 
    <style>
        #twitter
        {
            cursor: pointer;
        }
    </style>
    <script>
        $(function () {
            $('#beta').modal({
                keyboard: false, 
                backdrop: 'static'
            });
        });
    </script>
  </head>
  <body>
    <div id="beta" class="modal hide fade" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-header">
            <% if (isAuthenticated) { %>
            <h3>Dear <%= user.name %></h3>
            <% } else { %>
            <p>Dear Anonymous</p>
            You can <a href="/login">login</a> or <a href="mailto:tomasz@janczuk.org">request access.</a></p>
            <% } %>
        </div>
        <div class="modal-body">
            <% if (isAuthenticated) { %>
            <p>The application is now in a private Beta mode and you are not authorized to access it.
            You can <a href="/logout">logout</a> or <a href="mailto:tomasz@janczuk.org">request access.</a></p>
            <% } else { %>
            <p>The application is now in a private beta mode and you are not authorized to access it.
            You can <a href="/login">login</a> or <a href="mailto:tomasz@janczuk.org">request access.</a></p>
            <% } %>
            <p>Sincerely,<br>The W3 Team</p>
        </div>
    </div>
  </body>
</html>
